{
  "version": "2.1.2",
  "downloadUrl": "https://github.com/EntomoBrandsMR/better-update-utility-release/releases/download/v2.1.2/BUU.2.0.Setup.2.1.2.exe",
  "notes": "BUU 2.1.2 - fixes login being blocked by PestPac's loading overlay. PestPac added a MUI 'backdrop' over the login button, so the click never landed and login (and the license check) timed out after 30s with 'intercepts pointer events' - this broke runs on the latest patch. FIX: every login path (license check, each worker, the logout sweeper, and the setup/teardown runner) now waits for the overlay to clear before clicking, with a forced-click fallback. Small targeted patch on top of 2.1.1 (bulletproof logout, per-worker stop, wrong-sheet warning, drag/layout fixes, setup/teardown scope)."
}
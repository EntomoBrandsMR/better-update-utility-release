node_modules/
chromium/
dist/
build/
.cache/
out/
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.log
npm-debug.log*
.env
.env.local
credentials.enc
*.exe

# Workspace folders / one-off scripts (not part of BUU source)
scripts/
upcoming/
_asar-installed/

# API documentation — portal pages contain plaintext API key
# API documentation - contains plaintext API key, NEVER commit any of it
API DOCUMENTATION/

# Backup files
*.bak

# Excel lock/temp files
~$*

# Scratch / forensic artifacts (regenerable; never belong in git)
error-log.txt
_skip-analysis.json
_commitmsg-*.txt


# Build-critical files that must travel with the repo (learned 2026-07-04: repo couldn't rebuild itself)
!build/installer.nsh
!scripts/_validate-pool-worker.js
!scripts/_check-html-js.js

!build/snapshot-flows.js

# Generated at build time by build/snapshot-flows.js (a copy of C:\BUU\flows)
flows-bundle/

.ntfy-topic
